# Digital Wallet Extension - Environment Configuration
# Copy this file to .env and fill in your actual Firebase/Firestore credentials

# Firebase/Firestore Configuration
FIREBASE_API_KEY=your-firebase-api-key-here
FIREBASE_AUTH_DOMAIN=your-project-id.firebaseapp.com
FIREBASE_PROJECT_ID=your-project-id
FIREBASE_STORAGE_BUCKET=your-project-id.appspot.com
FIREBASE_MESSAGING_SENDER_ID=123456789012
FIREBASE_APP_ID=1:123456789012:web:abcdef123456

# How to get these values:
# 1. Go to https://console.firebase.google.com/
# 2. Create a new project or select existing one
# 3. Go to Project Settings > General tab
# 4. Scroll down to "Your apps" section
# 5. Click "Add app" and choose Web app (</>) 
# 6. Register app and copy the config values above
# 7. Go to Firestore Database section and create database
# 8. Set security rules to allow authenticated users:
#    rules_version = '2';
#    service cloud.firestore {
#        match /databases/{database}/documents {
#            match /wallets/{document} {
#               allow read, write: if true;
#            }
#        }
#    }
